import { CustonPipePipe } from './custon-pipe.pipe';

describe('CustonPipePipe', () => {
  it('create an instance', () => {
    const pipe = new CustonPipePipe();
    expect(pipe).toBeTruthy();
  });
});
